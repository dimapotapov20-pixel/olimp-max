-r requirements.txt

# The crawler reads public, born-digital admission pages. Its extraction is
# deliberately transparent and light: no browser runtime, ML model or OCR.
beautifulsoup4>=4.12,<5
openpyxl>=3.1,<4
pdfplumber>=0.11,<1
requests>=2.32,<3
